# Заметки

Эта страница демонстрирует свободный Markdown: цитату, список и inline code.

> Playable flow является симуляцией документации и не отправляет настоящий
> запрос.

Полезные наблюдения:

1. состояние ошибки называется `INVALID-INPUT`;
2. hotspot кнопки отправки хранится в `screens/hotspots.json`;
3. внешний переход ведёт только на HTTPS-страницу.
