# Безопасный Markdown

- Идентификатор: MOD-MARKDOWN
- Статус: Готово
- Владелец: Команда Toudocu
- Последнее обновление: 2026-08-10

Модуль один раз разбирает поддерживаемый CommonMark и GFM, создаёт
нормализованную структуру и безопасный HTML. Встроенное содержимое не
исполняется.

## Поддержка

Работают заголовки, абзацы, выделение, цитаты, ссылки, безопасные изображения,
списки, галочки, таблицы, зачёркивание, автоматические ссылки, код и ограниченные
блоки Mermaid. Внешний Markdown-сервис не нужен.

## Расположение в коде

- `internal/markdown/` — разбор, анализ и создание HTML;
- `internal/app/markdown_parse.go` — преобразование в модель проекта;
- `internal/app/markdown_render.go` — использование в портале;
- `internal/app/utils.go` — нормализация и экранирование.

## Границы

Внутреннее дерево Goldmark не входит в публичный Go API и JSON. Включены
CommonMark, Table, TaskList, Strikethrough и Linkify. Attributes, front matter,
footnotes, definition lists и typographer не поддерживаются. Репозиторные
ссылки и копирование ресурсов проверяет модель проекта.

## Бизнес-правила

### BR-MD-001: Пользовательский HTML считается ошибкой политики

Сырой блочный или встроенный HTML, в том числе в таблице, даёт
`forbidden-raw-html`. `check` и `build` завершаются неуспешно, а предварительный
просмотр и сравнение показывают исходник только как экранированный текст.

### BR-MD-002: Опасные протоколы и активные ресурсы блокируются

Опасная схема ссылки и HTML, JavaScript, SVG или XML из документации не
становятся активным ресурсом портала.

### BR-MD-003: Mermaid остаётся визуализацией

Разрешены `flowchart`, `stateDiagram-v2` и `sequenceDiagram` размером до
50 000 байт. Front matter и директивы Mermaid запрещены. Узлы и стрелки не
становятся требованиями, критериями или пунктами дорожной карты. Карта экранов
строится из таблиц `screens/SC-*.md`, а не из диаграммы.

## Инварианты

- Текст блока кода не разбирается как заголовок, ссылка или задача.
- Повторяющиеся заголовки получают разные якоря.
- Диапазон хранит смещение байтов от нуля и строку с колонкой от единицы.
- Метаданные — только первый верхнеуровневый маркированный список сразу после
  H1.
- Неизвестный синтаксис остаётся безопасным текстом.
- Локальные изображения ограничены безопасными растровыми форматами.
- Последовательность запросов описывается значимым `FLOW-*`; простая операция
  остаётся в API-контракте.
- Документ с Mermaid связан со сценарием или архитектурой.

## Стабильные интерфейсы

`check`, `build`, `serve`, Editor и Changes используют одну нормализованную
модель. Низкоуровневые функции разбора и отрисовки намеренно не экспортируются.

## Связанные сценарии

- [Сборка портала](../use-cases/build-portal.md)
- [Проверка документации](../use-cases/check-documentation.md)
